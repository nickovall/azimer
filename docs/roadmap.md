# AZIMER сайт — рабочий роадмап / бэклог

> Last updated: 2026-06-25
> Рабочий бэклог по сайту azimer.ru (контент + UX + тех. долг).
> Стратегический roadmap (5 фаз, клиентский, для Азамата) — отдельный
> документ `AZIMER_Roadmap_2026.pdf` в корне проекта, сюда не дублируем.

---

## ✅ Сделано (24 июня 2026)

- Фото галереи/объектов: «Каркас и контур», свап «Металлокаркас ↔ Сварочные»,
  кровля «в каске», склад вместо неба, карточки /projects «Производство» и
  «Технические сооружения» (сварщик в каске над резервуаром).
- Все новые фото оптимизированы (PNG ~2 МБ → JPG 120–190 КБ; у экспорта
  `images.unoptimized`).
- B2B-блок «Для бизнеса» на `/services`: НДС к вычету, счета-фактуры,
  закрывающие документы, безнал/договор, работа с юрлицами и ИП.
- Чистка: удалены дефолтные Next-шаблонные SVG и осиротевший `obj-sklad.jpg`.

---

## 🔐 Безопасность (25 июня) — см. `docs/audits/2026-06-25-security-review.md`

Закрыто:
- ✅ `kp_sessions` — утечка PII (анон-ключ читал сессии бота) → RLS + revoke, проверено пробой.
- ✅ SMS/спам-абуз → BEFORE INSERT рейт-лимитер `a_leads_rate_limit` (≥3/номер/10 мин, ≥12/час, ≥40/сутки).
- ✅ admin-api (CORS/токен), бот (fail-closed/webhook), RLS leads/документов — ре-верифицировано, ок.
- ✅ Старый `catalog-admin` — не задеплоен, действий не нужно.

Открыто:
- ⬜ **Подпись `/kp`** — HMAC серверных ссылок + пометка «предварительно» для неподписанных.
- ⬜ **`smooth-task` = ЖИВОЙ бот (НЕ удалять!)** — вебхук Telegram указывает на неё, а не на репо-`telegram-bot` (деплой-дрейф). Боевой исходник вне репо (бэкап `B:/dbtest/smooth-task-backup-2026-06-25.ts`). Задача: закоммитить исходник + cutover вебхука на `telegram-bot` с тестом, потом ретайр `smooth-task`.
- ⬜ **Follow-up к рейт-лимиту:** Cloudflare Turnstile + вставка за Edge Function (боты на входе, лиды не теряются на потолке).

---

## ⏸ Ждёт данных/контента от Азамата

- **Числа доверия в Stats** — заменить процессные (3 типа каркаса / 8 этапов /
  5+ типов / 24 ч) на реальные: объектов сдано, м² смонтировано, «на рынке
  с 2023». Блокер: нет собранного портфолио с цифрами.
  Подставлю за ~10 минут, как появятся числа. (NB: тянется к Фазе 2 PDF-роадмапа
  «ценовые якоря после сбора цен из КП».)
- **Кейсы на `/projects`** — карточки уже умеют показывать регион/площадь/срок/
  состав работ, но поля пустые → у всех «уточним по запросу». Блокер: кейсы не
  обработаны как кейсы.
- **Фото команды / Азамата** — блок «о людях» для доверия. Блокер: нет контента.
- **Источник для кейсов/чисел:** 6 благодарственных писем заказчиков в почте
  (МППМ, ООО БЛК, ИП Кокорин, Металл-Инвест, С-Материалс, МК-Решение).
  На сайт письма НЕ публикуем (решение владельца) — использовать как
  подтверждение фактов для кейсов/чисел.

---

## ⏳ Ждёт решения владельца

- **B2B-блок, 3 апгрейда** — добавить, если факты подтверждены:
  1. Акты **КС-2 / КС-3** (выдаёте именно эти формы?).
  2. **Тендеры / госзакупки** (реально участвуете, не только мониторите?).
  3. **Гарантия** на работы (даёте? срок?).

---

## 🔧 Технический / UX-бэклог (можно без Азамата)

- **Reveal гейтит контент на `opacity:0` до JS** — риск пустых экранов при сбое
  JS и для краулеров. Переделать: контент видим по умолчанию, анимировать только
  `transform`. (`components/ui/Reveal.tsx`)
- **Блок FAQ / возражения** — цена «от», сроки, этапы оплаты, гарантия
  (частично зависит от данных).
- **Карта / схема проезда** на `/contacts` (сейчас адрес только текстом).
- **robots** не закрывает `/console-x9p4m2` (некритично, защита на пароле).

---

## ❌ Решения (не делаем)

- **Благодарственные письма / отзывы на сайте** — не публиковать (решение
  владельца). Числа/факты из них использовать косвенно можно.
- **Анти-спам формы** — закрыто: клиентский антибот (honeypot/тайминг) + серверный
  рейт-лимитер `a_leads_rate_limit` (25.06). Усиление — Turnstile (см. §Безопасность).
