# Промпт для продолжения с Codex

> Скопируй блок ниже в Codex как стартовое сообщение. Он рассчитан на холодный
> старт на другом компе.

---

Ты продолжаешь работу над проектом **АЗИМЕР** (сайт + автоматизация для ООО
«АЗИМЕР», Красноярск — каркасные здания). Стек: Next.js 16 (static export →
GitLab Pages), Supabase (Postgres + Edge Functions), Telegram-бот. Рабочая
директория — `azimer-site/`.

## Шаг 0 — прочитай ПЕРЕД любым кодом (именно в этом порядке)
1. `AGENTS.md` — главное: это Next 16 со своими breaking changes, сверяйся с
   `node_modules/next/dist/docs/`, НЕ полагайся на обучающие данные.
2. `docs/SESSION_HANDOFF_2026-06-28.md` — что сделано в прошлой сессии и что
   осталось (твоя отправная точка).
3. `HANDOFF.md` (safe entry) и `docs/PROJECT_HANDOFF.md` (полная карта проекта).
4. По теме правки — соответствующий файл из «Ключевые файлы» в session-handoff.

## Контекст: что только что сделано (всё в проде)
В прошлой сессии (Claude) задеплоены: мобильная адаптация админки (нижний
таб-бар, карточки каталога), правка контактных полей лида (`update_lead_contact`)
и напоминания follow-up (`set_lead_followup` + миграция `follow_up_at`/
`follow_up_note`). Детали и список изменённых файлов — в
`docs/SESSION_HANDOFF_2026-06-28.md`.

## Твоя первая задача — верификация (клик-тест НЕ был сделан)
Админка за паролем, поэтому прошлая сессия не проверила новые фичи вживую.
Подними локально (`npm run dev` с `.env.local`) или проверь на проде в админке
`/console-x9p4m2/` и подтверди:
1. Напоминание: открыть лид → «Завтра 10:00» → появилось на дашборде в блоке
   «🔔 Напоминания на сегодня» и колокольчик 🔔 на канбане → снять → исчезло.
2. Контакты: секция «Контакт» → «✏ Изменить» → поправить email/телефон →
   Сохранить → обновилось (перезагрузка карточки показывает новое значение).
Если что-то не работает — чини в `app/console-x9p4m2/leads/view/page.tsx`,
`supabase/functions/admin-api/index.ts` (actions `update_lead_contact`,
`set_lead_followup`), при необходимости передеплой `admin-api`.

## Дальше (по приоритету; уточни у Ника ПЕРЕД стартом каждой)
- 🟡 История взаимодействий (timeline) вместо перезаписи заметок —
  `update_lead_notes` сейчас затирает весь текст.
- 🟡 Мультипользователи / назначение лида (под команду продаж Азамата).
- ⛔ Telegram/WhatsApp связь с клиентом — Ник сказал «есть сложности»,
  НЕ начинай без обсуждения.

## Жёсткие правила
- **Не ломать прод** (azimer.ru работает, бот принимает заявки).
- **Деплой-цепочка:** миграции БД → `supabase functions deploy admin-api
  --no-verify-jwt` → `git push gitlab main` (CI деплоит сайт+бот). Пушить в оба
  ремоута: `gitlab` (деплой) и `origin` (GitHub).
- **Прод-SQL** — через Supabase Management API (POST
  `/v1/projects/objgpsjyftdrhafapwvw/database/query` с access-токеном из
  `B:/dbtest/.tokens/supabase.txt`). Inline DB-пароли в старых `apply-*.mjs`
  протухли.
- **Перед «готово»**: `npx tsc --noEmit` + `npx next build` должны быть чисты.
- **Токены/секреты** — в `B:/dbtest/.tokens/` и `.env.local`, значения НЕ
  печатать и НЕ коммитить.
- В конце сессии обнови `docs/SESSION_HANDOFF_*.md` (новый файл с датой) и
  `HANDOFF.md`.

Начни с Шага 0 и подтверди, что прочитал, затем приступай к верификации.
